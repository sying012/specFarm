import { createTheme } from "@mui/material";
import styles from "../styles/mypage/MypageSub.module.css";

function MypageSub() {
  let content;

  

  return (
    <div>
      <div className={styles.mdfContainer}>{content}</div>
    </div>
  );
}

export default MypageSub;
